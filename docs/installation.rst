============
Installation
============

At the command line::

    $ easy_install django-dbes

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-dbes
    $ pip install django-dbes
